# Built binaries
/any
/any.mac
/bin/
/deps/
/dist/

# iOS xcframework local build artifacts (scripts/build-xcframework.sh).
# CI runners are fresh; these only appear on local runs.
/build/
/any.xcframework/

# Cached llama.cpp release tarballs (make llamacpp)
/third_party/llamacpp/cache/

# Go test/coverage artifacts
*.test
*.out
coverage.txt
coverage.html

# Editor scratch
.idea/
.vscode/
*.swp
*~

# Working prompt drafts (not part of the project surface)
prompt.md
prompt-*.md

# Local staging nodeconf fixture used by the e2e tests (not checked in;
# copy your own to the repo root). See internal/e2e/e2e_test.go.
/staging.yml
